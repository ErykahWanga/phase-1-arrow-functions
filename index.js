// Function expression
const divide = function() {
    return 2000 / 100;
  };
  
  // Arrow function with implicit return
  const square = x => x * x;
  
  // Arrow function with multiple parameters
  const add = (a, b) => a + b;
  